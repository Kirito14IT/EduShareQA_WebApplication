# GitHub 上传完整步骤指南

## 📋 准备工作

1. **确保已安装 Git**
   - 打开 PowerShell 或命令提示符
   - 输入 `git --version` 检查是否已安装
   - 如果未安装，请访问 https://git-scm.com/download/win 下载安装

2. **配置 Git 用户信息**（如果还没配置过）
   ```bash
   git config --global user.name "你的GitHub用户名"
   git config --global user.email "你的GitHub邮箱"
   ```

## 🚀 上传步骤（完整版）

### 步骤 1: 初始化 Git 仓库

在项目根目录（`E:\github\trae_projects\EduShareQA`）打开 PowerShell，执行：

```bash
cd e:\github\trae_projects\EduShareQA
git init
```

### 步骤 2: 添加远程仓库

```bash
git remote add origin https://github.com/Kirito14IT/EduShareQA_WebApplication.git
```

### 步骤 3: 查看要上传的文件

```bash
git status
```

这会显示所有未跟踪的文件。你应该看到很多文件，但**不会看到**以下目录（因为它们已被 .gitignore 忽略）：
- `node_modules/`
- `dist/`
- `__docx__/`
- `.vscode/`
- `.idea/`

### 步骤 4: 添加所有文件到暂存区

```bash
git add .
```

### 步骤 5: 提交文件

```bash
git commit -m "Initial commit: 添加项目初始代码"
```

### 步骤 6: 设置主分支名称（如果需要）

```bash
git branch -M main
```

### 步骤 7: 推送到 GitHub

```bash
git push -u origin main
```

**注意**：这一步会要求你输入 GitHub 用户名和密码（或 Personal Access Token）。

- 如果使用 HTTPS，GitHub 现在要求使用 **Personal Access Token** 而不是密码
- 获取 Token：GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic) → Generate new token
- 权限至少勾选：`repo`

### 步骤 8: 验证上传

访问 https://github.com/Kirito14IT/EduShareQA_WebApplication 查看你的代码是否已成功上传。

## 📝 后续更新代码的步骤

如果以后修改了代码，需要更新到 GitHub：

```bash
# 1. 查看修改的文件
git status

# 2. 添加修改的文件
git add .

# 3. 提交修改
git commit -m "描述你的修改内容"

# 4. 推送到 GitHub
git push
```

## ⚠️ 常见问题

### 问题 1: 提示需要认证
**解决方案**：使用 Personal Access Token 代替密码

### 问题 2: 文件太大上传失败
**解决方案**：检查 `.gitignore` 文件是否正确，确保大文件（如 `node_modules`）已被忽略

### 问题 3: 想要忽略某个文件但已经提交了
**解决方案**：
```bash
# 从 Git 中删除但保留本地文件
git rm --cached 文件名
# 然后提交
git commit -m "从 Git 中移除文件"
git push
```

### 问题 4: 想要查看哪些文件会被忽略
**解决方案**：
```bash
git status --ignored
```

## 📦 被忽略的目录说明

以下目录和文件**不会**被上传到 GitHub（已配置在 `.gitignore` 中）：

- ✅ `node_modules/` - 前端依赖包（很大，可以通过 npm install 重新安装）
- ✅ `dist/` - 构建产物（可以通过 npm run build 重新生成）
- ✅ `__docx__/` - Word 文档解压后的临时文件
- ✅ `.vscode/`, `.idea/` - IDE 配置文件
- ✅ `*.log` - 日志文件
- ✅ `.env` - 环境变量文件（可能包含敏感信息）
- ✅ `uploads/`, `files/` - 上传的文件目录

## 💡 提示

1. **首次上传可能需要一些时间**，因为文件较多
2. **确保网络连接稳定**
3. **如果上传中断**，可以重新执行 `git push`，Git 会继续上传
4. **建议定期提交代码**，不要等到项目完成才提交

